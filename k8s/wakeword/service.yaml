apiVersion: v1
kind: Service
metadata:
  name: diva-wakeword
  namespace: diva
spec:
  type: ClusterIP
  ports:
    - port: 8003
      targetPort: http
      name: http
  selector:
    app: diva-wakeword
