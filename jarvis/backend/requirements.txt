# Jarvis Pi Gateway
# Lightweight - no model inference runs here
# All STT/TTS/LLM calls are forwarded to the big machine via HTTP

fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9
httpx>=0.27.0
pydantic>=2.0.0
python-dotenv>=1.0.0
