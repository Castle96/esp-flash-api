# STT microservice - runs on big machine (port 8001)
# CPU now:
WHISPER_MODEL=Systran/faster-whisper-large-v3
WHISPER_DEVICE=cpu
WHISPER_COMPUTE_TYPE=int8

# GPU later (swap these):
# WHISPER_DEVICE=cuda
# WHISPER_COMPUTE_TYPE=float16
