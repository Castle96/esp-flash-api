services:
  jarvis-backend:
    build: ./jarvis/backend
    container_name: jarvis-backend
    restart: unless-stopped
    ports:
      - "8000:8000"
    env_file:
      - ./jarvis/backend/.env
    volumes:
      - ./jarvis/voices:/voices:ro
