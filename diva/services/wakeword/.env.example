# Wake word models to load (comma-separated)
WAKEWORD_MODELS=alexa,hey_jarvis

# Detection threshold (0.0 - 1.0, lower = more sensitive)
WAKEWORD_THRESHOLD=0.5

# Directory to store custom wake word embeddings
CUSTOM_WAKEWORD_DIR=/data/custom_wakewords
